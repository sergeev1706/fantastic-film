<div class="container">
  <nav class="nav">
    <div>
      {{#unless isList}}
      <a href="/page/{{pageNum}}">назад</a>
      {{/unless}}
    </div>

    {{#if isList}}
    <form action="/filter/" class="header_form" method="POST">

      <select name="year_select" id="">
        <option value="">year</option>
        {{#each years}}
        <option>{{.}}</option>
        {{/each}}
      </select>

      <select name="country_select" id="">
        <option value="">country</option>
        {{#each countrys}}
        <option value="{{.}}">{{.}}</option>
        {{/each}}
      </select>

      <select name="rating_select" id="">
        <option value="">rating</option>
        {{#each ratings}}
        <option value="{{.}}">{{.}}</option>
        {{/each}}
      </select>

      <button type="submit">применить</button>

    </form>
    {{/if}}

  </nav>
</div>